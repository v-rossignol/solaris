# Solaris — local development
#
# Dev server:  http://localhost:3003/solaris/
# API prefix:   /infinity/*  → proxied to http://localhost:4000 (Infinity Server)
#
# Port and proxy are configured in vite.config.ts when the Vite scaffold is in place.
# See AGENTS.md and ../AGENTS.md for the full monorepo local dev stack.
